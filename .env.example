# AgriShield — Root Environment Variables
# Copy to .env in each subdirectory as needed

SENTINEL_HUB_API_KEY=
STELLAR_ADMIN_SECRET_KEY=
STELLAR_NETWORK=testnet
SOROBAN_CONTRACT_ID=
MONGODB_URI=
NEXT_PUBLIC_API_URL=http://localhost:3001
